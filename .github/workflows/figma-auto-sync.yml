# DEPRECATED — replaced by scheduled-token-build.yml
# This workflow used the Figma REST API which requires a paid Figma plan (returns 403 on free plans).
# Token sync is now handled via Token Studio plugin push + scheduled-token-build.yml.
# Keeping this file as documentation only. Cron removed to avoid duplicate runs.

name: Figma Auto-Sync (Deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "This workflow is deprecated."
          echo "Token sync is handled by scheduled-token-build.yml"
          echo "Use Token Studio plugin to push tokens to GitHub."
