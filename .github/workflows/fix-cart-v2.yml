# DEPRECATED — one-off cart stories v2 fix, no longer needed.
# Cart component is stable. Ongoing fixes go through normal PRs.

name: Fix Cart Stories v2 (Deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Deprecated. Cart stories have been fixed."
