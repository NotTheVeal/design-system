# DEPRECATED — one-off v1.1.0 npm publish, no longer needed.
# Use publish.yml for all future npm releases.

name: Publish v1.1.0 to npm (Deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Deprecated. v1.1.0 has been published. Use publish.yml for future releases."
