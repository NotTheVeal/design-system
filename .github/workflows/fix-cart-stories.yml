# DEPRECATED — one-off cart stories fix, no longer needed.
# Replaced by the main design-system.yml pipeline.
# Removed push trigger to prevent errors on every commit.

name: Fix Cart Stories (Deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "This workflow is deprecated. Use design-system.yml pipeline."
