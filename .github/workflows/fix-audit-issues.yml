# DEPRECATED — one-off Storybook/Figma audit fix script, no longer needed.
# All 14 audit issues have been resolved. Ongoing fixes go through normal PRs.

name: Fix Audit Issues (Deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "This one-off audit fix workflow is deprecated. All issues resolved."
