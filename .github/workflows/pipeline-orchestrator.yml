# DEPRECATED — pipeline-orchestrator.yml
#
# This workflow was an incomplete stub (cut off mid-definition, referenced a
# non-existent Python validate script). design-system.yml now owns this
# responsibility: validate tokens → lint → test → build Storybook.
#
# DO NOT ADD TRIGGERS HERE — this file is intentionally inert.

name: Pipeline Orchestrator (DEPRECATED)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: echo "This workflow is deprecated. See design-system.yml."
