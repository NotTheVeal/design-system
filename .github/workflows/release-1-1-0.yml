# DEPRECATED — one-off v1.1.0 release script, no longer needed.
# Future releases are handled by publish.yml (workflow_dispatch with version input).

name: Release 1.1.0 (Deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "This one-off release workflow is deprecated. Use publish.yml for releases."
