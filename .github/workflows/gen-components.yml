# DEPRECATED — one-off script for generating color-scheme component variants.
# Color scheme migration is complete. Use generate-component.yml for new components.

name: Generate Color-Scheme Components (Deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Deprecated. Color scheme components have been generated. Use generate-component.yml."
