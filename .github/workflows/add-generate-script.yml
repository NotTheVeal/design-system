# DEPRECATED — one-off bootstrap script for generate-component.js, no longer needed.
# The script now lives permanently at scripts/generate-component.js.

name: Bootstrap generate-component script (Deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Deprecated. generate-component.js already exists at scripts/generate-component.js"
