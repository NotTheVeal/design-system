# DEPRECATED — audit-report.yml
#
# This workflow was an incomplete draft (setup only, no audit steps).
# Token drift monitoring is handled by token-drift-monitor.yml.
#
# DO NOT ADD TRIGGERS HERE — this file is intentionally inert.

name: Audit Report (DEPRECATED)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: echo "This workflow is deprecated. See token-drift-monitor.yml for token drift monitoring."
