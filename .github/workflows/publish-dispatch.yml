# DEPRECATED — superseded by publish.yml which has version input and better error handling.
# Use publish.yml (workflow_dispatch with version input) for all npm releases.

name: Publish to npm - Manual (Deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Deprecated. Use publish.yml for npm releases — it accepts a version input."
